task_id: task_02_decision
category: C_episodic
fixture: fixtures/history_repo
base_commit: "a97390a8c7b7f17c90d57598538192aebc6b6ecb"
prompt_file: prompt.md
eval_command:
  - python
  - -c
  - |
    from auth.tokens import validate_token
    assert validate_token("token-abc") is True
    assert validate_token("v2:token-abc") is True
    assert validate_token("bad") is False
    assert validate_token("") is False
timeout_seconds: 120
tags: ["episodic", "decision"]
preseed:
  - type: decision
    source: agent
    timestamp: "2026-02-15T09:00:00+00:00"
    repo: history_repo
    symbol: validate_token
    file: auth/tokens.py
    payload:
      decision: "token format v2: new tokens carry a v2: version prefix"
      detail: "validate_token must accept legacy token- and v2:token- forms; reject everything else"
