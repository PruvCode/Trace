task_id: task_04_page_offbyone
category: A_control
fixture: fixtures/pager_repo
base_commit: "bc80e0936d617a56371059c9d7030598837c6080"
prompt_file: prompt.md
eval_command:
  - python
  - -c
  - |
    from store.paging import paginate
    rows = list(range(100))
    assert paginate(rows, 1, 10) == list(range(10))
    assert paginate(rows, 2, 10) == list(range(10, 20))
    assert paginate(rows, 10, 10) == list(range(90, 100))
    assert paginate(rows, 1) == list(range(50))
timeout_seconds: 120
tags: ["control", "pagination", "single-file"]
