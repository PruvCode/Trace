# Template for a benchmark task. Copy this directory to create a real task.
# Loader rules: all keys below are required; unknown keys are rejected.
task_id: template_task
category: A_control  # one of: A_control, B_structural, C_episodic, D_staleness
fixture: fixtures/toy_repo
base_commit: "0000000000000000000000000000000000000000"  # HEAD SHA from scripts/seed_toy_repo.py
prompt_file: prompt.md
eval_command: ["python", "-m", "pytest", "tests/test_session.py", "-q"]
timeout_seconds: 120
tags: ["template"]
