"""Example task test (template only; not executed by the benchmark).

Real evaluator tests live with the fixture repo (fixtures/toy_repo/tests/)
and run inside the isolated workspace via the task's eval_command.
"""


def test_example():
    assert True
