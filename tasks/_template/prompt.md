# Task prompt template

Describe the task here. These exact bytes are the agent prompt for every
configuration (fairness rule: the base prompt must be byte-identical across
configs; only a fixed memory-tool addendum may be appended for memory configs).

State what "done" means in terms of observable code behavior, not in terms of
what the agent should claim.
