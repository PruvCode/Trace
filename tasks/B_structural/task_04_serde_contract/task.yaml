task_id: task_04_serde_contract
category: B_structural
fixture: fixtures/api_repo
base_commit: "ec8302d893bc682d9bdfff2a541ba2078a143a11"
prompt_file: prompt.md
eval_command:
  - python
  - -c
  - |
    from api.serialize import to_wire
    from api.deserialize import from_wire
    user = {"user_id": "u1", "name": "ann"}
    wire = to_wire(user)
    assert wire == {"userId": "u1", "name": "ann"}
    assert from_wire(wire) == user
    assert from_wire({"userId": "u2", "name": "bob"}) == {"user_id": "u2", "name": "bob"}
timeout_seconds: 120
tags: ["structural", "serde-contract", "multi-file"]
